<% layout('layouts/boilerplate')%>

    <div class="row g-0 ratio mb-3 ratio-4x3">
        <img src="<%= product.images[0] %>" class="img-fluid object-fit-cover">
    </div>
    <h1><%= product.name %></h1>
    <h2>Price: €<%= product.price %></h2>
    <h3>Colors: <%= product.colors %></h3>
    <h3>Sizes: <%= product.sizes %></h3>
    <div class="mb-3">
        <button class="btn btn-dark ">ADD TO CART </button>
    </div>
    <footer class="mb-3">
        <a href="/products">All Products</a>
    </footer>
